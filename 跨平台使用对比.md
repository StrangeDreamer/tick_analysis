# 跨平台使用对比

## 📱 平台支持一览表

| 功能 | macOS/Linux | Windows |
|------|-------------|---------|
| **启动脚本** | ✅ `./run.sh` | ✅ `run_win.bat` 或 `run_win.ps1` |
| **虚拟环境** | ✅ `venv/` | ✅ `venv\` |
| **Python命令** | `python3` | `python` 或 `py` |
| **测试工具** | ✅ 支持 | ✅ 支持 |

---

## 🚀 快速使用对比

### 创建虚拟环境

| 平台 | 命令 |
|------|------|
| **macOS/Linux** | `python3 -m venv venv` |
| **Windows** | `python -m venv venv` |

### 激活虚拟环境

| 平台 | 命令 |
|------|------|
| **macOS/Linux** | `source venv/bin/activate` |
| **Windows CMD** | `venv\Scripts\activate.bat` |
| **Windows PowerShell** | `venv\Scripts\Activate.ps1` |

### 安装依赖

| 平台 | 命令 |
|------|------|
| **macOS/Linux** | `pip3 install -r requirements.txt` |
| **Windows** | `pip install -r requirements.txt` |

### 运行程序

| 模式 | macOS/Linux | Windows CMD | Windows PowerShell |
|------|-------------|-------------|--------------------|
| **单次执行** | `./run.sh test` | `run_win.bat test` | `.\run_win.ps1 test` |
| **循环执行** | `./run.sh loop` | `run_win.bat loop` | `.\run_win.ps1 loop` |
| **强制循环** | `./run.sh force` | `run_win.bat force` | `.\run_win.ps1 force` |

### 测试工具

| 工具 | macOS/Linux | Windows |
|------|-------------|---------|
| **API连接测试** | `python3 test_connection.py` | `python test_connection.py` |
| **快速测试** | `python3 quick_test.py` | `python quick_test.py` |

---

## 📝 详细文档

- **macOS/Linux 用户**：查看 `使用说明.md`
- **Windows 用户**：查看 `Windows使用指南.md`

---

## ⚡ 一键启动命令

### macOS/Linux
```bash
cd /path/to/tick_analysis
./run.sh loop      # 推荐：循环执行（仅开市时间）
```

### Windows CMD
```cmd
cd C:\path\to\tick_analysis
run_win.bat loop   REM 推荐：循环执行（仅开市时间）
```

### Windows PowerShell
```powershell
cd C:\path\to\tick_analysis
.\run_win.ps1 loop  # 推荐：循环执行（仅开市时间）
```

---

## 🔄 文件路径差异

| 说明 | macOS/Linux | Windows |
|------|-------------|---------|
| **路径分隔符** | `/` | `\` |
| **虚拟环境激活** | `venv/bin/activate` | `venv\Scripts\activate.bat` |
| **当前目录** | `./run.sh` | `run_win.bat` |
| **家目录** | `~/` | `%USERPROFILE%\` |

---

## ✅ 系统已准备好

无论您使用什么平台，都有对应的脚本和文档！

| 平台 | 启动脚本 | 状态 |
|------|---------|------|
| macOS | `run.sh` | ✅ 已创建 |
| Linux | `run.sh` | ✅ 已创建 |
| Windows CMD | `run_win.bat` | ✅ 已创建 |
| Windows PowerShell | `run_win.ps1` | ✅ 已创建 |

---

## 💡 推荐选择

- **macOS/Linux 用户** → 使用 `./run.sh`
- **Windows 新手** → 使用 `run_win.bat`（CMD）
- **Windows 高级用户** → 使用 `run_win.ps1`（PowerShell）

所有脚本功能完全一致！🎉
